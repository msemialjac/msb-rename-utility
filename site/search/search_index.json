{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"msb_rename_utility","text":"<p>A small Flask + vanilla-JS bulk file renamer, inspired by Better Rename Utility (BRU) and M\u00e9tamorphose 2, clean-room reimplemented so the code is free of copyleft obligations and free of legacy Python-2 baggage.</p>"},{"location":"#design-in-one-paragraph","title":"Design in one paragraph","text":"<p>A rename run is a pipeline: a list of ops applied in order to each input filename. Each op is a pure function <code>(name, idx, ctx, params) -&gt; name</code>. The UI builds the pipeline as JSON and POSTs it to <code>/api/preview</code>; the server returns old/new pairs and a <code>collision</code> flag. Nothing is written to disk until <code>/api/apply</code> is called \u2014 which in this scaffold is deliberately stubbed until the undo-log is implemented.</p>"},{"location":"#why-another-rename-tool","title":"Why another rename tool","text":"<ul> <li>Free: MIT-licensed, no ads, no upgrade nags, no macOS-only lock-in.</li> <li>Scriptable: the pipeline is JSON, so the same config runs from CLI or web UI.</li> <li>Auditable: every op is a ~30-line pure function, documented one-to-one with   its docs page. No hidden behavior.</li> </ul>"},{"location":"#sibling-project","title":"Sibling project","text":"<p>The visual language (Tokyo Night palette, card layout) mirrors <code>disk-dashboard</code>. The two apps are meant to feel like a suite of single-purpose Flask tools.</p>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#install-run","title":"Install &amp; run","text":"<pre><code>git clone &lt;repo&gt; &amp;&amp; cd msb_rename_utility\npython -m venv .venv &amp;&amp; source .venv/bin/activate\npip install -r requirements.txt\npython app.py\n</code></pre> <p>Open http://127.0.0.1:5051.</p>"},{"location":"getting-started/#your-first-rename","title":"Your first rename","text":"<ol> <li>Paste filenames into the textarea \u2014 one per line.</li> <li>Click + case, set <code>mode = lower</code>, <code>scope = both</code>.</li> <li>Click preview. You'll see old/new pairs.</li> <li><code>apply</code> is not wired yet (scaffold) \u2014 for now the tool is preview-only.</li> </ol>"},{"location":"getting-started/#docs-build","title":"Docs build","text":"<pre><code>pip install mkdocs-material\nmkdocs serve    # live preview at http://127.0.0.1:8000\nmkdocs build    # static site into ./site/\n</code></pre>"},{"location":"concepts/collisions/","title":"Collisions","text":"<p>A collision is any situation where applying the pipeline would cause one rename to clobber another file. Apply refuses to run if any row has <code>collision: true</code>.</p>"},{"location":"concepts/collisions/#what-counts-as-a-collision","title":"What counts as a collision","text":"<p>The preview endpoint flags a row as <code>collision: true</code> when either is true:</p> <ol> <li>Self-collision: two or more input files produce the same <code>new</code> name.</li> <li>External collision: a row's <code>new</code> name equals the old name of a    different file in the input set \u2014 i.e. the rename would land on a file    that's about to be renamed itself.</li> </ol>"},{"location":"concepts/collisions/#what-isnt-flagged-yet","title":"What isn't flagged (yet)","text":"<ul> <li>A new name that collides with a file already on disk but not in the   input set \u2014 we don't scan the directory at preview time (preview doesn't   receive <code>dir</code>). Apply's safety is limited to the input set. A future   enhancement is to pass <code>dir</code> optionally to preview for a deep check.</li> </ul>"},{"location":"concepts/collisions/#how-collisions-are-resolved","title":"How collisions are resolved","text":"<p>They aren't, automatically. The user is shown the <code>\u26a0 collision</code> marker and expected to fix the pipeline (add a <code>numbering</code> step, insert a stem suffix, etc.). A future op <code>collision_auto_append</code> could add <code>\" (2)\"</code>, <code>\" (3)\"</code>, \u2026 to collided rows \u2014 BRU has this; we don't yet.</p>"},{"location":"concepts/collisions/#why-this-is-safe-by-construction","title":"Why this is safe by construction","text":"<p>Even if a caller bypassed the preview UI and hit <code>/api/apply</code> directly with a colliding pipeline, the server re-computes preview and refuses with HTTP 409. The client preview is advisory; the server is authoritative.</p>"},{"location":"concepts/pipeline/","title":"Pipeline model","text":"<p>A rename run is a list of ops applied in order to each filename independently.</p> <pre><code>for each filename:\n    for each op in pipeline:\n        filename = op(filename, idx, ctx, params)\n</code></pre> <ul> <li><code>idx</code> is the zero-based position of the file in the original input list.   Numbering ops use this to assign sequence numbers.</li> <li><code>ctx</code> is a per-run dict shared across all ops and all files. Reserved for   future ops that need to stash derived state (e.g. EXIF dates read once).</li> <li>Ops never touch disk. They transform strings. This makes them trivially   unit-testable and makes preview literally the same code path as apply.</li> </ul>"},{"location":"concepts/pipeline/#json-shape","title":"JSON shape","text":"<pre><code>{\n  \"files\": [\"IMG_0001.JPG\", \"IMG_0002.JPG\"],\n  \"pipeline\": [\n    { \"op\": \"case\",      \"params\": { \"mode\": \"lower\", \"scope\": \"both\" } },\n    { \"op\": \"numbering\", \"params\": { \"start\": 1, \"pad\": 3, \"sep\": \"_\" } }\n  ]\n}\n</code></pre>"},{"location":"concepts/pipeline/#why-a-pipeline-instead-of-one-big-operation","title":"Why a pipeline instead of one big operation?","text":"<p>BRU and M\u00e9tamorphose both went this route for the same reason: rename intents are compositional. \"Lowercase the extension and strip a prefix and append a sequence number\" is three small, well-defined steps rather than one parameter-bloated mega-op. Every op stays ~30 lines, one docs page, one set of examples.</p>"},{"location":"concepts/preview-vs-apply/","title":"Preview vs apply","text":"<p>The app has two endpoints that look similar but differ in one critical way: preview touches nothing, apply mutates disk.</p>"},{"location":"concepts/preview-vs-apply/#preview-post-apipreview","title":"Preview (<code>POST /api/preview</code>)","text":"<ul> <li>Accepts <code>files</code> (list of basenames) and <code>pipeline</code>.</li> <li>Runs the pipeline in-memory and returns <code>[{old, new, unchanged, collision}]</code>.</li> <li>No <code>dir</code> needed. The endpoint doesn't know or care where the files live.</li> <li>Safe to call on every keystroke.</li> </ul>"},{"location":"concepts/preview-vs-apply/#apply-post-apiapply","title":"Apply (<code>POST /api/apply</code>)","text":"<ul> <li>Requires <code>dir</code> in addition to <code>files</code> and <code>pipeline</code>.</li> <li>Re-runs preview server-side \u2014 the UI's preview is hint, not promise.</li> <li>Refuses if any row has <code>collision: true</code>, any new name is empty / contains   <code>/</code>, any file is missing on disk, or <code>dir</code> isn't a directory.</li> <li>Writes an undo-log JSON sidecar into <code>dir</code> before the first rename.</li> <li>Performs renames in two phases: old \u2192 <code>.__rename_&lt;uuid&gt;_&lt;i&gt;.tmp</code>, then tmp \u2192   new. This makes the intermediate state collision-free regardless of order.</li> <li>Returns <code>{applied, undo_log, rows}</code>.</li> </ul>"},{"location":"concepts/preview-vs-apply/#why-two-endpoints-instead-of-one","title":"Why two endpoints instead of one","text":"<p>The same code path (<code>run_pipeline</code>) computes both. Splitting the endpoints makes apply's safety gates explicit and makes preview cacheable / retriable without side effects. If a single endpoint with a <code>dry_run</code> flag feels nicer, consider it a layering choice \u2014 this project chose separation because the safety story is simpler to audit.</p>"},{"location":"concepts/preview-vs-apply/#undo","title":"Undo","text":"<p><code>POST /api/undo</code> takes the <code>undo_log</code> path the server returned, replays the renames in reverse (also two-phase), and archives the log with a <code>.done</code> suffix so it can't be replayed again by accident. If you lose the path, the log is sitting in the target directory as <code>.rename-undo-&lt;timestamp&gt;.json</code>.</p>"},{"location":"ops/case/","title":"<code>case</code> \u2014 change letter case","text":"<p>Changes the letter case of the filename. This is the exemplar op page: every other op page uses exactly this structure (What / Parameters / Examples / Edge cases).</p>"},{"location":"ops/case/#what-it-does","title":"What it does","text":"<p>Applies one of four case transforms (<code>lower</code>, <code>upper</code>, <code>title</code>, <code>sentence</code>) to either the stem, the extension, or both. The dot separating stem and extension is never touched.</p>"},{"location":"ops/case/#parameters","title":"Parameters","text":"name type default description <code>mode</code> <code>\"lower\"</code> | <code>\"upper\"</code> | <code>\"title\"</code> | <code>\"sentence\"</code> <code>\"lower\"</code> How to transform the targeted text. <code>scope</code> <code>\"stem\"</code> | <code>\"ext\"</code> | <code>\"both\"</code> <code>\"stem\"</code> Which part of the filename to transform. <p><code>sentence</code> capitalizes only the first character and lowercases the rest \u2014 <code>\"hello WORLD\"</code> \u2192 <code>\"Hello world\"</code>.</p>"},{"location":"ops/case/#examples","title":"Examples","text":"lower stemlower bothtitle stem <pre><code>{ \"op\": \"case\", \"params\": { \"mode\": \"lower\", \"scope\": \"stem\" } }\n</code></pre> before after <code>IMG_0001.JPG</code> <code>img_0001.JPG</code> <code>Vacation.TXT</code> <code>vacation.TXT</code> <pre><code>{ \"op\": \"case\", \"params\": { \"mode\": \"lower\", \"scope\": \"both\" } }\n</code></pre> before after <code>IMG_0001.JPG</code> <code>img_0001.jpg</code> <pre><code>{ \"op\": \"case\", \"params\": { \"mode\": \"title\", \"scope\": \"stem\" } }\n</code></pre> before after <code>my vacation notes.txt</code> <code>My Vacation Notes.txt</code>"},{"location":"ops/case/#edge-cases","title":"Edge cases","text":"<ul> <li>No extension: <code>scope: \"ext\"</code> with a name like <code>README</code> is a no-op \u2014 the   empty extension is transformed to the empty string.</li> <li>Multiple dots: only the last dot separates stem from extension, so   <code>archive.tar.gz</code> \u2192 stem <code>archive.tar</code>, ext <code>.gz</code>. <code>title</code> stem would yield   <code>Archive.Tar.gz</code> (yes, the inner dot gets capitalized by <code>str.title()</code> \u2014 this   is Python's behavior and documented as such).</li> <li>Unicode: <code>\"\u00df\".upper()</code> is <code>\"SS\"</code> in Python, which grows the filename by one   character. This is a Unicode fact, not a bug \u2014 the op faithfully reflects it.</li> </ul>"},{"location":"ops/case/#see-also","title":"See also","text":"<ul> <li><code>replace</code> \u2014 if you want to change casing of only part of the name.</li> <li>Concepts \u203a Pipeline \u2014 how <code>case</code> combines with other ops.</li> </ul>"},{"location":"ops/insert/","title":"<code>insert</code> \u2014 put a literal string at a position in the stem","text":"<p>Inserts <code>text</code> at a character position inside the stem. The extension is always preserved.</p>"},{"location":"ops/insert/#what-it-does","title":"What it does","text":"<p>Inserts <code>text</code> at index <code>at</code> within the stem. <code>at</code> may be:</p> <ul> <li>a non-negative integer \u2192 position from the start (<code>0</code> = prepend)</li> <li>a negative integer \u2192 position from the end (<code>-1</code> = before last char)</li> <li>the string <code>\"end\"</code> \u2192 append to the stem (before the dot-extension)</li> </ul>"},{"location":"ops/insert/#parameters","title":"Parameters","text":"name type default description <code>text</code> string <code>\"\"</code> The text to insert. Empty \u2192 no-op. <code>at</code> int | <code>\"end\"</code> <code>0</code> Position within the stem."},{"location":"ops/insert/#examples","title":"Examples","text":"prependappend to stem (extension preserved)negative index <pre><code>{ \"op\": \"insert\", \"params\": { \"text\": \"2024_\", \"at\": 0 } }\n</code></pre> before after <code>photo.jpg</code> <code>2024_photo.jpg</code> <pre><code>{ \"op\": \"insert\", \"params\": { \"text\": \"_edited\", \"at\": \"end\" } }\n</code></pre> before after <code>photo.jpg</code> <code>photo_edited.jpg</code> <code>archive.tar.gz</code> <code>archive.tar_edited.gz</code> (last dot is the ext separator) <pre><code>{ \"op\": \"insert\", \"params\": { \"text\": \"-\", \"at\": -2 } }\n</code></pre> before after <code>photo.jpg</code> <code>pho-to.jpg</code>"},{"location":"ops/insert/#edge-cases","title":"Edge cases","text":"<ul> <li>Index past end: clamped to the stem length (<code>at=999</code> on a 5-char stem   acts like <code>\"end\"</code>).</li> <li>Negative past start: clamped to <code>0</code>.</li> <li>No stem: inserting into a name like <code>.gitignore</code> (empty stem) puts the   text before the dot, e.g. <code>\"X\" + \".gitignore\"</code> \u2192 <code>\"X.gitignore\"</code>.</li> </ul>"},{"location":"ops/numbering/","title":"<code>numbering</code> \u2014 sequential numbers","text":"<p>Assigns a sequential number to each file based on its position in the input list (<code>idx</code>). Commonly used as the last step of a pipeline, after names have been normalized.</p>"},{"location":"ops/numbering/#what-it-does","title":"What it does","text":"<p>For each file, computes <code>n = start + idx * step</code>, optionally zero-pads it to <code>pad</code> digits, then places it before or after the stem, separated by <code>sep</code>. The extension is preserved.</p>"},{"location":"ops/numbering/#parameters","title":"Parameters","text":"name type default description <code>start</code> int <code>1</code> Number assigned to the first input file. <code>step</code> int <code>1</code> Delta between consecutive files. <code>pad</code> int <code>0</code> Zero-pad width. <code>0</code> = no padding. <code>position</code> <code>\"suffix\"</code> | <code>\"prefix\"</code> <code>\"suffix\"</code> Where to place the number relative to stem. <code>sep</code> string <code>\"_\"</code> Separator between stem and number."},{"location":"ops/numbering/#examples","title":"Examples","text":"zero-padded suffixprefix with custom separatorstep by 10 (gap-leaving) <pre><code>{ \"op\": \"numbering\", \"params\": { \"start\": 1, \"pad\": 3 } }\n</code></pre> idx before after 0 <code>photo.jpg</code> <code>photo_001.jpg</code> 1 <code>cover.jpg</code> <code>cover_002.jpg</code> 2 <code>back.jpg</code> <code>back_003.jpg</code> <pre><code>{ \"op\": \"numbering\", \"params\": { \"start\": 10, \"pad\": 2, \"position\": \"prefix\", \"sep\": \"-\" } }\n</code></pre> idx before after 0 <code>photo.jpg</code> <code>10-photo.jpg</code> 1 <code>cover.jpg</code> <code>11-cover.jpg</code> <pre><code>{ \"op\": \"numbering\", \"params\": { \"start\": 10, \"step\": 10, \"pad\": 3 } }\n</code></pre> idx after 0 <code>a_010.jpg</code> 1 <code>a_020.jpg</code> 2 <code>a_030.jpg</code>"},{"location":"ops/numbering/#edge-cases","title":"Edge cases","text":"<ul> <li><code>idx</code> is pre-sort: the zero-based position in the input list as received.   If you want numbering in a specific order (alphabetical, by mtime), sort on   the client side before sending the list to <code>/api/preview</code>.</li> <li><code>pad</code> too small: if the computed number exceeds <code>pad</code> digits, it's   rendered at its natural width \u2014 we never truncate numbers. <code>pad=2</code> with   <code>start=99</code> yields <code>99</code>, <code>100</code>, <code>101</code>, \u2026</li> <li>Negative <code>step</code>: allowed. <code>start=100, step=-1</code> counts down.</li> </ul>"},{"location":"ops/numbering/#see-also","title":"See also","text":"<ul> <li>Concepts \u203a Pipeline \u2014 on why <code>idx</code> is per-file,   not per-op.</li> </ul>"},{"location":"ops/regex/","title":"<code>regex</code> \u2014 pattern find/replace","text":"<p>Python <code>re.sub</code> over the filename. Supports groups and backreferences.</p>"},{"location":"ops/regex/#what-it-does","title":"What it does","text":"<p>Applies <code>re.sub(pattern, repl, target, flags=...)</code> to the filename (or stem). <code>repl</code> supports Python's <code>\\1</code>, <code>\\g&lt;name&gt;</code>, etc.</p>"},{"location":"ops/regex/#parameters","title":"Parameters","text":"name type default description <code>pattern</code> string (Python regex) <code>\"\"</code> Empty \u2192 no-op. Compile errors return HTTP 400. <code>repl</code> string <code>\"\"</code> Replacement. Supports <code>\\1</code>, <code>\\2</code>, <code>\\g&lt;name&gt;</code>. <code>flags</code> list of chars <code>[]</code> Any of <code>\"i\"</code> (ignorecase), <code>\"m\"</code> (multiline), <code>\"s\"</code> (dotall). <code>scope</code> <code>\"name\"</code> | <code>\"stem\"</code> <code>\"name\"</code> Whether the extension participates in the match."},{"location":"ops/regex/#examples","title":"Examples","text":"rename by capture groupstrip trailing ' (1)' / ' (2)' duplicatesextract date from mixed prefix (case-insensitive) <pre><code>{ \"op\": \"regex\", \"params\": { \"pattern\": \"IMG_(\\\\d+)\", \"repl\": \"photo_\\\\1\" } }\n</code></pre> before after <code>IMG_0001.JPG</code> <code>photo_0001.JPG</code> <code>IMG_42-edit.png</code> <code>photo_42-edit.png</code> <pre><code>{ \"op\": \"regex\", \"params\": { \"pattern\": \" \\\\(\\\\d+\\\\)(?=\\\\.[^.]+$)\", \"repl\": \"\", \"scope\": \"name\" } }\n</code></pre> before after <code>report (1).pdf</code> <code>report.pdf</code> <code>notes (12).txt</code> <code>notes.txt</code> <code>vacation.txt</code> <code>vacation.txt</code> <pre><code>{\n  \"op\": \"regex\",\n  \"params\": {\n    \"pattern\": \"^(IMG|DSC)_(\\\\d{4})(\\\\d{2})(\\\\d{2}).*\",\n    \"repl\":    \"\\\\2-\\\\3-\\\\4\",\n    \"flags\":   [\"i\"]\n  }\n}\n</code></pre> before after <code>img_20240413_9.jpg</code> <code>2024-04-13.jpg</code> (if <code>scope=\"stem\"</code>; otherwise the ext is also rewritten)"},{"location":"ops/regex/#edge-cases","title":"Edge cases","text":"<ul> <li>Invalid pattern: the server returns HTTP 400 with the <code>re.error</code> message.</li> <li>Empty <code>pattern</code>: no-op (we don't want empty-match insertion semantics).</li> <li>Partial matches: <code>re.sub</code> replaces all non-overlapping matches by default.   Use <code>^...$</code> anchors if you need \"exactly the whole name.\"</li> <li>BRU-flavor regex: BRU uses its own PCRE-ish dialect. This op is Python   <code>re</code> \u2014 document differences rather than emulate. Most common BRU patterns   work unchanged; PCRE-only features (e.g. recursion <code>(?R)</code>, possessive   quantifiers <code>*+</code>) do not.</li> </ul>"},{"location":"ops/regex/#see-also","title":"See also","text":"<ul> <li><code>replace</code> \u2014 if you don't need patterns, prefer literal replace.</li> </ul>"},{"location":"ops/replace/","title":"<code>replace</code> \u2014 literal find/replace","text":"<p>Literal (non-regex) substring replacement. Use <code>regex</code> when you need patterns, groups, or anchors.</p>"},{"location":"ops/replace/#what-it-does","title":"What it does","text":"<p>Replaces every occurrence of <code>find</code> in the filename with <code>with</code>. Optionally case-insensitive. Optionally scoped to just the stem (leaving the extension alone).</p>"},{"location":"ops/replace/#parameters","title":"Parameters","text":"name type default description <code>find</code> string <code>\"\"</code> Literal substring to match. Empty \u2192 no-op. <code>with</code> string <code>\"\"</code> Replacement text. Empty string deletes matches. <code>case_sensitive</code> bool <code>true</code> When <code>false</code>, matches regardless of case. <code>scope</code> <code>\"name\"</code> | <code>\"stem\"</code> <code>\"name\"</code> <code>\"stem\"</code> protects the extension from changes."},{"location":"ops/replace/#examples","title":"Examples","text":"strip a prefixrename a word, case-insensitiveprotect the extension <pre><code>{ \"op\": \"replace\", \"params\": { \"find\": \"IMG_\", \"with\": \"\" } }\n</code></pre> before after <code>IMG_0001.JPG</code> <code>0001.JPG</code> <code>vacation.txt</code> <code>vacation.txt</code> (unchanged) <pre><code>{ \"op\": \"replace\", \"params\": { \"find\": \"draft\", \"with\": \"final\", \"case_sensitive\": false } }\n</code></pre> before after <code>Draft_report.pdf</code> <code>final_report.pdf</code> <code>DRAFT-notes.md</code> <code>final-notes.md</code> <pre><code>{ \"op\": \"replace\", \"params\": { \"find\": \"photo\", \"with\": \"X\", \"scope\": \"stem\" } }\n</code></pre> before after <code>photo.photo</code> <code>X.photo</code>"},{"location":"ops/replace/#edge-cases","title":"Edge cases","text":"<ul> <li>Empty <code>find</code>: returns the filename unchanged (we don't expand empty-match   insertion-between-every-char \u2014 that's a regex concern, not literal replace).</li> <li>Overlapping matches: resolved left-to-right, like Python's <code>str.replace</code>.</li> <li>Case-insensitive: implemented without regex so <code>find</code> can contain regex   metacharacters (<code>.</code>, <code>*</code>, <code>(</code>, etc.) without escaping.</li> </ul>"},{"location":"ops/replace/#see-also","title":"See also","text":"<ul> <li><code>regex</code> \u2014 for patterns and backreferences.</li> <li><code>case</code> \u2014 if you only need to change letter case.</li> </ul>"}]}